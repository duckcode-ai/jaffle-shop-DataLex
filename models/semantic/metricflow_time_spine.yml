version: 2

models:
  - name: metricflow_time_spine
    description: Daily time spine required by MetricFlow-compatible semantic metrics.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: Calendar date at day granularity.
        granularity: day
